export default {
  // custom for login requests (can only access a single loginToken)
  login: 'login',

  // custom role
  admin: 'admin',

  // all users have these by default
  self: 'self',
  user: 'user',
};
